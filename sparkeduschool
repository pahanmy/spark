<!DOCTYPE html>
<html lang="ms">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SPARK - Pengurusan Data Prestasi Sekolah</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: 'Arial', sans-serif; background-color: #f8f9fa; }
        .btn { transition: background-color 0.3s, transform 0.2s; }
        .btn:hover:not(:disabled) { transform: translateY(-2px); }
        .logo-pulse { animation: pulse-animation 2s infinite; }
        
        .data-cell {
            padding: 8px 12px;
            text-align: left;
            font-size: 0.875rem;
        }
        .score-input {
            width: 80px;
            text-align: center;
            padding: 4px;
            border: 1px solid #e5e7eb;
            border-radius: 4px;
            font-family: 'Arial', sans-serif;
            font-size: 0.875rem;
        }
        .score-input:focus {
            border-color: #3b82f6; 
            outline: none;
            box-shadow: 0 0 0 1px #3b82f6;
        }
    </style>
</head>
<body class="bg-gray-50 flex flex-col min-h-screen">

    <header class="bg-white shadow-sm">
        <div class="max-w-screen-2xl mx-auto py-4 px-4 sm:px-6 lg:px-8 flex flex-col sm:flex-row justify-between sm:items-center gap-4">
            <div class="flex items-center gap-3 self-start sm:self-center">
                <div class="bg-indigo-600 p-2 rounded-lg logo-pulse"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="text-white"><path d="M4 4H8V8H4V4Z" fill="currentColor"></path><path d="M4 16H8V20H4V16Z" fill="currentColor"></path><path d="M16 4H20V8H16V4Z" fill="currentColor"></path><path d="M10 4H14V6H10V4Z" fill="currentColor"></path><path d="M10 8H12V10H10V8Z" fill="currentColor"></path><path d="M12 12H14V14H12V12Z" fill="currentColor"></path><path d="M10 16H12V18H10V16Z" fill="currentColor"></path><path d="M14 16H16V18H14V16Z" fill="currentColor"></path><path d="M16 12H18V14H16V12Z" fill="currentColor"></path><path d="M16 8H20V10H16V8Z" fill="currentColor"></path><path d="M12 18H14V20H12V18Z" fill="currentColor"></path><path d="M4 10H6V14H4V10Z" fill="currentColor"></path></svg></div>
                <div><h1 class="text-2xl font-bold text-indigo-600">SPARK</h1><p class="text-sm text-gray-500">Pengurusan Data Prestasi Sekolah</p></div>
            </div>
             <a href="index.html" class="btn bg-gray-200 text-gray-800 font-semibold py-2 px-4 rounded-lg shadow-md hover:bg-gray-300">Halaman Utama</a>
        </div>
    </header>
    <main class="max-w-screen-2xl mx-auto py-6 sm:px-6 lg:px-8 flex-grow w-full">
        <div class="max-w-7xl mx-auto p-4 md:p-8 bg-white shadow-lg rounded-xl">
             <header class="mb-6 pb-4 border-b border-blue-200">
                <h1 class="text-3xl font-extrabold text-blue-800">📊 Data Prestasi Sekolah Miri</h1>
                <p class="text-sm text-gray-600 mt-1">Pengurusan data TOV/ETR mengikut nama sekolah.</p>
            </header>
             <div id="loading-spinner" class="text-center p-8 text-blue-500">
                <svg class="animate-spin h-8 w-8 mx-auto" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                <p class="mt-2">Memuatkan data sekolah...</p>
            </div>

            <div id="school-data-content" class="overflow-x-auto hidden">
                 <div class="mb-4 flex justify-between items-center">
                    <input type="text" id="school-search" placeholder="Cari nama sekolah..." class="pl-4 pr-4 py-2 border border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 w-full max-w-sm">
                    <p class="text-sm text-gray-600">Jumlah Sekolah: <span id="school-count" class="font-bold">0</span></p>
                </div>
                <table class="min-w-full border-collapse">
                    <thead class="bg-blue-600 text-white sticky top-0">
                        <tr>
                            <th class="data-cell w-1/12 border-r border-blue-500">Bil.</th>
                            <th class="data-cell w-5/12 border-r border-blue-500 text-left">Nama Sekolah</th>
                            <th class="data-cell w-2/12 border-r border-blue-500 text-center">2024 (TOV)</th>
                            <th class="data-cell w-2/12 border-r border-blue-500 text-center">2025 (ETR)</th>
                            <th class="data-cell w-2/12 text-center">Tindakan</th>
                        </tr>
                    </thead>
                    <tbody id="school-list-body" class="divide-y divide-gray-200">
                        </tbody>
                </table>
                 <p id="no-data-message" class="text-center text-gray-500 py-6 hidden">Tiada sekolah ditemui.</p>
            </div>
        </div>
    </main>

    <footer class="text-center py-4 mt-8 border-t border-gray-200">
        <p class="text-sm text-gray-600">Dibangunkan oleh <a href="https://pahanmy.github.io/works/" target="_blank" class="font-semibold text-indigo-600 hover:underline">Pahan</a></p>
        <p class="text-xs text-gray-500">Versi 2.7 (Data Prestasi Sekolah)</p>
    </footer>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInAnonymously, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
        import { getFirestore, collection, onSnapshot, doc, setDoc, query, orderBy, getDoc } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

        // Konfigurasi Firebase (Guna konfigurasi dari fail data.html)
        const firebaseConfig = { apiKey: "AIzaSyAEBe1XF7cqaxkTNBZBoo3DBJYIuchOEkM", authDomain: "myqrhadir.firebaseapp.com", projectId: "myqrhadir", storageBucket: "myqrhadir.appspot.com", messagingSenderId: "799691133747", appId: "1:799691133747:web:835a5e3e9413eace806446" };
        
        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);
        const auth = getAuth(app);
        const appId = firebaseConfig.projectId;

        // Koleksi data
        const schoolsCollectionRef = collection(db, `artifacts/${appId}/public/data/spark_schools`);
        // Koleksi BARU untuk menyimpan Skor Sekolah
        const performanceCollectionPath = `artifacts/${appId}/public/data/spark_school_performance`; 
        const performanceCollectionRef = collection(db, performanceCollectionPath);


        let allSchools = [];
        let allPerformanceData = {};
        
        // --- AUTENTIKASI ---
        function authenticateAndLoad() {
             onAuthStateChanged(auth, async (user) => {
                if (!user) {
                    try {
                        await signInAnonymously(auth);
                    } catch (error) {
                        console.error("Gagal daftar masuk tanpa nama:", error);
                    }
                }
                // Setelah disahkan (atau jika pengguna sudah ada), mulakan muatan data
                loadDataListeners();
            });
        }
        
        // --- DATA LISTENERS ---
        function loadDataListeners() {
            // 1. Dapatkan Senarai Sekolah (Master List)
            const qSchools = query(schoolsCollectionRef, orderBy("order", "asc"));
            onSnapshot(qSchools, (snapshot) => {
                allSchools = snapshot.docs.map(doc => ({ 
                    id: doc.id, 
                    name: doc.data().name,
                    // Kita gunakan nama sekolah sebagai ID dokumen prestasi
                    performanceId: doc.data().name.trim().toUpperCase().replace(/[^A-Z0-9]/g, '_')
                }));
                document.getElementById('school-count').textContent = allSchools.length;
                renderSchoolTable();
            }, (error) => {
                console.error("Ralat memuatkan data sekolah:", error);
            });

            // 2. Dapatkan Data Prestasi (Skor TOV/ETR)
            onSnapshot(performanceCollectionRef, (snapshot) => {
                allPerformanceData = {};
                snapshot.forEach(doc => {
                    allPerformanceData[doc.id] = doc.data();
                });
                renderSchoolTable();
            }, (error) => {
                 console.error("Ralat memuatkan data prestasi:", error);
            });
            
            // Sembunyikan spinner selepas percubaan muatan
            document.getElementById('loading-spinner').classList.add('hidden');
            document.getElementById('school-data-content').classList.remove('hidden');
        }

        // --- RENDER TABLE ---
        function renderSchoolTable() {
            const listBody = document.getElementById('school-list-body');
            const searchInput = document.getElementById('school-search').value.toLowerCase();
            
            const filteredSchools = allSchools.filter(s => s.name.toLowerCase().includes(searchInput));

            if (filteredSchools.length === 0) {
                 listBody.innerHTML = `<tr><td colspan="5" class="text-center text-gray-500 py-4">Tiada sekolah ditemui bagi carian ini.</td></tr>`;
                return;
            }

            let htmlContent = filteredSchools.map((school, index) => {
                const perfId = school.performanceId;
                const perfData = allPerformanceData[perfId] || { tov: 0.00, etr: 0.00 };

                return `
                    <tr class="hover:bg-gray-50">
                        <td class="data-cell text-center text-gray-500">${index + 1}</td>
                        <td class="data-cell text-sm font-medium text-gray-900">${school.name}</td>
                        
                        <td class="data-cell text-center">
                            <input type="number" 
                                   step="0.01" 
                                   min="0" 
                                   max="100"
                                   value="${(perfData.tov || 0).toFixed(2)}" 
                                   onchange="updateScore('${perfId}', 'tov', this.value)"
                                   onblur="this.value = parseFloat(this.value).toFixed(2)"
                                   class="score-input"
                            />
                        </td>
                        
                        <td class="data-cell text-center">
                             <input type="number" 
                                   step="0.01" 
                                   min="0" 
                                   max="100"
                                   value="${(perfData.etr || 0).toFixed(2)}" 
                                   onchange="updateScore('${perfId}', 'etr', this.value)"
                                   onblur="this.value = parseFloat(this.value).toFixed(2)"
                                   class="score-input font-bold text-indigo-600"
                            />
                        </td>
                        
                        <td class="data-cell text-center">
                             <a href="prestasi.html?sekolah=${encodeURIComponent(school.name)}" 
                                class="btn inline-flex items-center gap-1 text-xs font-bold text-white bg-indigo-600 py-2 px-3 rounded-lg shadow-md hover:bg-indigo-700">
                                <span>Laporan</span>
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor"><path d="M11 3a1 1 0 100 2h2.586l-6.293 6.293a1 1 0 101.414 1.414L15 6.414V9a1 1 0 102 0V4a1 1 0 00-1-1h-5z" /><path d="M5 5a2 2 0 00-2 2v8a2 2 0 002 2h8a2 2 0 002-2v-3a1 1 0 10-2 0v3H5V7h3a1 1 0 000-2H5z" /></svg>
                            </a>
                        </td>
                    </tr>
                `;
            }).join('');
            
            listBody.innerHTML = htmlContent;
        }

        // --- FUNGSI KEMAS KINI SKOR ---
        async function updateScore(performanceId, fieldName, value) {
            const numericValue = parseFloat(value);
            
            if (isNaN(numericValue) || numericValue < 0 || numericValue > 100) {
                console.error("Nilai skor tidak sah.");
                alert("Nilai skor mesti antara 0.00 hingga 100.00.");
                return;
            }
            
            const docRef = doc(performanceCollectionRef, performanceId);
            
            try {
                // Gunakan setDoc dengan merge: true untuk memastikan dokumen dicipta jika ia tidak wujud
                // tanpa menimpa medan lain.
                await setDoc(docRef, { [fieldName]: numericValue }, { merge: true });
                console.log(`Skor ${fieldName} untuk ${performanceId} berjaya dikemaskini.`);
            } catch (e) {
                console.error(`Ralat mengemaskini skor ${fieldName} untuk ${performanceId}:`, e);
                alert(`Gagal kemaskini skor. Sila semak keizinan Firebase. ${e.message}`);
                // Muat semula jadual untuk menunjukkan data lama jika simpan gagal
                renderSchoolTable(); 
            }
        }
        
        // --- INISIALISASI ---
        authenticateAndLoad();
        
        // --- DEDAHKAN FUNGSI KE WINDOW ---
        document.getElementById('school-search').addEventListener('input', renderSchoolTable);
        Object.assign(window, {
            updateScore 
        });

    </script>
</body>
</html>
